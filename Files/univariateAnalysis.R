

names <- names(churnData)

plot(churnData$gender, main = "Gender")

plot(churnData$SeniorCitizen, main = "Senior Citizens")

plot(churnData$Partner, main = "Partners")

plot(churnData$Dependents, main = "Dependents")

plot(churnData$tenure, main = "Tenure")

plot(churnData$PhoneService, main = "Phone Service")

plot(churnData$MultipleLines, main = "Multiple Lines")

plot(churnData$InternetService, main = "Internet Service")

plot(churnData$OnlineSecurity, main = "Online Security")

plot(churnData$OnlineBackup, main = "Online Backup")

plot(churnData$DeviceProtection, main = "Device Protection")

plot(churnData$TechSupport, main = "Tech Support")

plot(churnData$StreamingTV, main = "Streaming TV")

plot(churnData$StreamingMovies, main = "Streaming Movies")

plot(churnData$Contract, main = "Contract Type")

plot(churnData$PaperlessBilling, main = "Paperless Billing")

plot(churnData$PaymentMethod, main = "Payment Method Type")

plot(churnData$MonthlyCharges, main = "Monthly Charges")

plot(churnData$TotalCharges, main = "Total Charges")

plot(churnData$Churn, main = "Churn")






